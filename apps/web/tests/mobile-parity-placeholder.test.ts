/**
 * Placeholder: dedicated mobile web-parity tests (permissions, routing, menu)
 * were removed when syncing from origin/main. Reintroduce focused tests here
 * or in apps/mobile when parity checks are needed again.
 */
import { describe, it } from "node:test";
import assert from "node:assert/strict";

describe("mobile / web parity", () => {
  it("placeholder — suite to be expanded when parity specs return", () => {
    assert.ok(true);
  });
});
